const difference = (x,y) => x-y;
const multy = (first ,second ,third) => first*second*third;
const getage = (person) => person.age;
const student = {name: 'anata', age: 45}
const age = getage(student)
console.log(age) 